services:
  robo-rick:
    build: .
    container_name: robo-rick
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist channel settings across restarts
      - ./adminchannels.json:/app/adminchannels.json
      - ./welcomechannels.json:/app/welcomechannels.json
